name: Health Check

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v4
      - id: foo
        uses: atorresgleza/health-check
        with:
          url: ${{secrets.URL_TEST}}
